{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "node --test server.test.js",
    "lint": "node --check server.js utils.js server.test.js",
    "build": "echo 'Build OK — syntax verified' && node --check server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  }
}
